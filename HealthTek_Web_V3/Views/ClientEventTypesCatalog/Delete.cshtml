@model HealthTek_Shared_Libraries.ClientEventTypesCatalog

<div class="text-center">
    <i class="fas fa-exclamation-triangle fa-5x text-warning pb-2"></i>
    <h5>
        Warning: Deleting this environmental change can cause the application to not display certain views. Are you sure you want to delete the environmental change <code>@Html.DisplayFor(model => model.EventType)</code> ?
    </h5>
    <hr />
    <form asp-action="Delete" asp-route-id="@Model.ClientEventTypesCatalogId" class="text-right row">
        <input type="text" class="form-control col-md-7 mt-1 ml-2" id="walletName" placeholder="Enter Event Type Change..." name="Name">
        <input type="submit" value="Delete" class="btn btn-danger col-md-4 ml-2 " disabled id="deleteBtn" />
    </form>
</div>

<script>
    $('input[name="Name"]').keyup(function () {
        var Name = @Html.Raw(Json.Serialize(Model.EventType));
        var name = $(this).val();
        if (Name == name) {
            document.getElementById('deleteBtn').disabled = false;
        } else {
            document.getElementById('deleteBtn').disabled = true;
        }
    });
</script>