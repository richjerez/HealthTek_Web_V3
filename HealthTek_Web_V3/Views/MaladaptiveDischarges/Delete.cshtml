@model HealthTek_Shared_Libraries.MaladaptiveDischarges

<div class="text-center">
    <i class="fas fa-exclamation-triangle fa-5x text-warning pb-2"></i>
    <h5>
        Warning: You are about to delete a maladaptive discharge. Are you sure you want to delete the discgarge for maladaptive <code>@Html.DisplayFor(model => model.FkMaladaptives.MaladaptiveName)</code> ?
    </h5>
    <hr />
    <form asp-action="Delete" asp-route-id="@Model.MaladaptiveDischargesId" class="text-right row">
        <input type="text" class="form-control col-md-7 mt-1 ml-2" id="walletName" placeholder="Enter Maladaptive..." name="Name">
        <input type="submit" value="Delete" class="btn btn-danger col-md-4 ml-2 " disabled id="deleteBtn" />
    </form>
</div>

<script>
    $('input[name="Name"]').keyup(function () {
        var Name = @Html.Raw(Json.Serialize(Model.FkMaladaptives.MaladaptiveName));
        var name = $(this).val();
        if (Name == name) {
            document.getElementById('deleteBtn').disabled = false;
        } else {
            document.getElementById('deleteBtn').disabled = true;
        }
    });
</script>